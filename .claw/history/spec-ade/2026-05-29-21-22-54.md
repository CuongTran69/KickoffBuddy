---
claw: Spec ade
source: telegram
timestamp: 2026-05-29T21:22:54.066659+07:00
duration_ms: 3297486
success: true
request_id: fb2eabb5-b64f-433c-967f-9bfa7050fb49
---

## User
<!-- 2026-05-29T13:27:56.700+00:00 -->

osf autopilot làm tiếp feat B fan etiquette guide nhé

## Assistant
<!-- 2026-05-29T21:22:54.066659+07:00 -->

Đã đọc đủ codebase. Bức tranh rõ ràng. Đây là assessment:

## Autopilot: Exploration Complete

**Type**: feat
**What**: Thêm Fan Etiquette Guide — một feature mới hoàn toàn với list screen (7 chủ đề) + detail screen, JSON asset bundled, repository + provider theo pattern Rule Cards, route mới `/etiquette/:id`, và entry point từ Settings screen (không thêm tab nav vì nav bar đã có 5 tab, thêm tab 6 sẽ làm vỡ layout sliding indicator).

**Key decisions**:
- Navigation entry: thêm tile trong Settings screen (Resources section) — không thêm tab mới vì `_PremiumFloatingNavBar` dùng `destinations.length` để tính `itemWidth` và sliding indicator position, thêm tab 6 sẽ làm vỡ animation. Pattern tương tự Vocabulary hiện tại (accessible từ Settings).
- Data model: `EtiquetteTip` model mới, JSON asset `assets/data/etiquette_tips.json`, repository pattern giống `RuleCardRepository` (lazy load, in-memory cache).
- UI: list screen dùng `ListView` với `_EtiquetteTile` cards, detail screen dùng `SingleChildScrollView` với body text — tương tự `RuleCardDetailScreen` nhưng đơn giản hơn (không có level filter, không có EN/VI toggle vì content chỉ có VI).
- Routes: `/etiquette` (list) + `/etiquette/:id` (detail) — outside shell, push over nav bar.
- Content: 7 chủ đề từ `docs/future/03-fan-etiquette.md`, bundled vào JSON asset.

**Pipeline**: Full — multi-file (new feature area: model, repository, 2 screens, JSON asset, routes, i18n, settings integration)✓ Created proposal✓ Created design✓ Created specs`assets/data/` already declared as wildcard — no pubspec change needed.✅ Spec created: feat-fan-etiquette-guide## ✅ Autopilot Complete

**Change**: feat-fan-etiquette-guide
**Pipeline**: spec ✓ → implement ✓ → verify ✓ → archive ✓
**Verify rounds**: 1

---

Fan Etiquette Guide đã hoàn thành. Tóm tắt:

- **`etiquette_tips.json`** — 7 tips bundled (no_spoil, watch_together, explain_rules, sports_bar, when_to_cheer, referee_reactions, time_wasting)
- **Data layer** — `EtiquetteTip` model + `EtiquetteRepository` (lazy load, in-memory cache, pattern giống RuleCardRepository)
- **2 screens** — `EtiquetteListScreen` + `EtiquetteDetailScreen` với đầy đủ loading/error/not-found states
- **Routing** — `/etiquette` và `/etiquette/:id` outside shell
- **Settings** — Resources card giờ có cả Vocabulary lẫn Etiquette tile
- **i18n** — 5 keys trong cả vi + en

Không thêm tab nav mới — entry point qua Settings giữ nav bar 5 tab nguyên vẹn.
