---
claw: Spec ade
source: telegram
timestamp: 2026-05-29T19:36:03.900674+07:00
duration_ms: 897874
success: true
request_id: 67d508b8-9b38-4e8d-99bd-1e0cf09326a0
---

## User
<!-- 2026-05-29T12:21:05.913+00:00 -->

osf autopilot thực hiện feat sleep plan đi

## Assistant
<!-- 2026-05-29T19:36:03.900674+07:00 -->

Autopilot: classifying as **feat**Đã có đủ context. Tôi có bức tranh rõ ràng về codebase. Đây là plan đầy đủ:

## Autopilot: Exploration Complete

**Type**: feat
**What**: Thêm Sleep Plan card vào Match Detail screen — khi trận có giờ kickoff local từ 22:00–05:00, hiển thị card gợi ý 3 chế độ (Late Watcher / Balanced / Healthy Replay) kèm disclaimer. Nút "Healthy Replay" tích hợp với Replay Planner đã có.

**Key decisions**:
- Placement: card trong `_MatchDetailBody`, sau Match Info card, trước action buttons — dựa trên pattern hiện tại của screen
- Trigger: `localKickoff.hour >= 22 || localKickoff.hour < 5` — pure logic, không cần provider mới
- Replay integration: nút "Healthy Replay" gọi `showReplayPlannerDialog(context, match)` — reuse existing function
- State: stateless widget, không cần Riverpod state mới — chỉ đọc `localKickoff` đã có sẵn trong `_MatchDetailBodyState`
- i18n: thêm strings vào cả `app_en.arb` và `app_vi.arb`
- Styling: dùng `_PremiumSettingsCard`-style container với `Icons.bedtime_outlined` — match design system hiện tại

**Pipeline**: Full — multi-file (screen + 2 ARB files + new widget file), new feature với UI mới, cần spec để track✓ Created proposal✓ Created design✓ Created specs✅ Spec created: feat-sleep-plan## ✅ Autopilot Complete

**Change**: feat-sleep-plan
**Pipeline**: spec ✓ → implement ✓ → verify ✓ → archive ✓
**Verify rounds**: 1

---

Sleep Plan đã được build và archive. Tóm tắt những gì đã làm:

- **`sleep_plan_card.dart`** — widget mới, glassmorphism style, 3 mode rows (Late Watcher / Balanced / Healthy Replay), CTA mở Replay Planner, disclaimer
- **`match_detail_screen.dart`** — thêm trigger `isLateNight` và insert card giữa match info và action buttons
- **`app_en.arb` + `app_vi.arb`** — 9 keys mới, đầy đủ cả 2 ngôn ngữ

Card chỉ hiện khi kickoff local từ 22:00–04:59 và trận chưa bắt đầu — không ảnh hưởng gì đến các trận ban ngày.
