---
claw: Spec ade
source: telegram
timestamp: 2026-05-29T19:03:13.755583+07:00
duration_ms: 1215671
success: true
request_id: 9700a4d9-a495-4179-a60a-3d5bd3368f93
---

## User
<!-- 2026-05-29T11:42:58.078+00:00 -->

osf autopilot fix bug hết những gì bạn tìm đc đi

## Assistant
<!-- 2026-05-29T19:03:13.755583+07:00 -->

Autopilot: classifying as **fix**Đang đọc codebase để hiểu rõ từng bug trước khi tạo spec.Đã có đủ context. Bây giờ tôi có bức tranh đầy đủ về tất cả 7 bugs. Đây là assessment:

## Autopilot: Exploration Complete

**Type**: fix
**What**: Fix 7 pre-launch bugs — Android permissions, disclaimer, exact-alarm UX dialog, notification channel ID consistency, onboarding timezone button no-op, standings offline cache, and Isar requireValue safety.
**Key decisions**:
- Notification channel ID: giữ `match_reminders` (code) vì đây là giá trị đã được publish trong spec `match-reminders/spec.md` line 22 — spec ghi `match_reminders` là đúng, không phải `kickoff_reminders`. Không cần đổi.
- Onboarding timezone "Change" button: ẩn button thay vì implement full override (Sprint 2 feature chưa có scope) — đây là fix đúng nhất, không để no-op visible.
- Standings cache: dùng `keepAlive()` (Riverpod `ref.keepAlive()`) thay vì thêm Isar collection mới — minimal change, đúng pattern.
- Isar requireValue: wrap trong `AsyncValue.when` guard ở `app.dart` để đảm bảo Isar ready trước khi screens render.
- Exact-alarm dialog: thêm vào `reminder_sheet.dart` flow, check `canScheduleExactAlarms` trên Android 12+.

**Starting pipeline**: Full (multi-file, platform-specific, launch-critical)✓ Created proposal

Now getting instructions for design and specs in parallel:✓ Created design

Now creating the three spec files:✓ Created specs (android-permissions, settings-about, match-reminders delta)

Now getting tasks instructions:✅ Spec created: fix-prelaunch-bugs0 CRITICALs, 2 WARNINGs. Cả hai warnings đều có giá trị — đặc biệt là missing `<receiver>` cho boot-completed. Tôi sẽ fix cả hai trước khi archive.Cả hai warnings đã được fix. Chạy verify lần 2:0 CRITICALs, 0 WARNINGs. Tiến hành archive:## ✅ Autopilot Complete

**Change**: fix-prelaunch-bugs
**Pipeline**: spec ✓ → implement ✓ → verify ✓ (2 rounds) → archive ✓
**Verify rounds**: 2

---

Tất cả 7 bugs đã được fix:

| # | Bug | Fix |
|---|-----|-----|
| 1 | AndroidManifest thiếu `SCHEDULE_EXACT_ALARM` | Đã thêm permission + `ScheduledNotificationBootReceiver` |
| 2 | AndroidManifest thiếu `RECEIVE_BOOT_COMPLETED` | Đã thêm permission |
| 3 | Không có UX dialog xin exact-alarm (Android 12+) | Thêm dialog với deep-link đến `ACTION_REQUEST_SCHEDULE_EXACT_ALARM` |
| 4 | Disclaimer "unofficial" thiếu trong Settings | Thêm About card ở cuối Settings screen |
| 5 | Onboarding "Change" timezone button là no-op | Xóa button, giữ comment Sprint 2 |
| 6 | `isarInstanceProvider.requireValue` có thể throw | Đổi sang `Provider<Isar?>` với `asData?.value` |
| 7 | `standingsProvider` refetch mỗi lần navigate | Thêm `ref.keepAlive()` |

Verify round 2 phát hiện thêm 2 issues (missing boot receiver + wrong settings page) và đã fix luôn trước khi archive.
